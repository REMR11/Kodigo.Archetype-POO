# Hotel Reservation System Configuration
app.name=Hotel Reservation System
app.version=1.0.0

# Logging
logging.level.com.hotel=DEBUG
logging.level.root=INFO

# Database (para futuras extensiones)
# db.url=jdbc:h2:mem:testdb
# db.username=sa
# db.password=
